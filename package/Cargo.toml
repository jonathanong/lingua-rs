[package]
name = "lingua-rs-node"
version = "0.1.1"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
lingua-rs = { path = "../crate" }
napi = { version = "3", features = ["napi4", "serde-json"] }
napi-derive = "3"

[build-dependencies]
napi-build = "2"
